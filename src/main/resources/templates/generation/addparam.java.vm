package ${cfg.paramPackage};

import io.swagger.annotations.ApiModel;
import io.swagger.annotations.ApiModelProperty;
import lombok.*;
import java.util.Date;
import lombok.Data;
import java.time.LocalDate;
import java.time.LocalDateTime;
import com.yunke.admin.common.base.BaseAddParam;
#if(${swagger})
import io.swagger.annotations.ApiModel;
import io.swagger.annotations.ApiModelProperty;
#end

/**
 *
 * @author ${author}
 * @date ${date}
 */
@Data
@NoArgsConstructor
@AllArgsConstructor
@Builder
@ApiModel(value = "${entity}AddParam对象", description = "${entity}AddParam对象")
public class ${entity}AddParam extends BaseAddParam {

    #foreach($field in ${table.fields})
        #if(${field.propertyName} != "id"&&${field.propertyName} != "createTime" && ${field.propertyName} != "updateTime"&&${field.propertyName} != "createBy" && ${field.propertyName} != "updateBy")
        @ApiModelProperty(value = "${field.comment}")
        private ${field.propertyType} ${field.propertyName};

        #end
    #end
}